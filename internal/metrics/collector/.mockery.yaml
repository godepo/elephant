with-expecter: True
dir: ./
mockname: "Mock{{.InterfaceName}}"
filename: "mock_{{.InterfaceName}}_test.go"
outpkg: collector
packages:
  github.com/godepo/elephant:
    config:
      all: False
    interfaces:
      Counter:
        config:
      Histogram:
        config:
      ErrorsLogInterceptor:
        config:
      Interceptor:
        config:
      HistogramCollector:
        config:
      CounterCollector:
        config: